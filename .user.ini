; PHP upload limits for giangbai (LiteSpeed / cPanel read this file).
; If hosting still shows 2M, raise limits in cPanel → Select PHP Version → Options.
upload_max_filesize = 32M
post_max_size = 36M
max_execution_time = 300
memory_limit = 256M
# Antigravity Workflow

- Antigravity IDE và CLI (`agy`) được phép trực tiếp khảo sát (`/survey`), lập kế hoạch (`docs/handoff/PLAN.md`), triển khai code (`implement`) và kiểm thử (`/verify`) theo yêu cầu trực tiếp của User.
- Bỏ cơ chế phân vai cấm sửa source khi User yêu cầu tự code.
- Nguồn sự thật (file):
  + `docs/handoff/PLAN.md`
  + `docs/handoff/IMPLEMENT.md`
  + `docs/handoff/VERIFY.md`
